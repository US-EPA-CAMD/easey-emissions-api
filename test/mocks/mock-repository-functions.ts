/**
 * Mocks functions which are given by parent repository
 */
export const mockRepositoryFunctions = {
  create: jest.fn(),
  save: jest.fn(),
  find: jest.fn(),
  insert: jest.fn(),
  upsert: jest.fn(),
  update: jest.fn(),
};
